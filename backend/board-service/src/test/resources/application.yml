server:
  port: 8083

spring:
  application:
    name: board-service
  
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  
  h2:
    console:
      enabled: true

file:
  upload-dir: ./test-uploads

eureka:
  client:
    enabled: false
