# Lucide React Icons 종류 및 사용법

## 📦 설치

```bash
npm install lucide-react
```

---

## 🎨 주요 카테고리별 아이콘

### 1. 네비게이션 & UI

```javascript
import {
  Home,           // 🏠 홈
  Menu,           // ☰ 메뉴
  X,              // ✕ 닫기
  ChevronDown,    // ⌄ 아래 화살표
  ChevronUp,      // ⌃ 위 화살표
  ChevronLeft,    // ‹ 왼쪽 화살표
  ChevronRight,   // › 오른쪽 화살표
  ArrowLeft,      // ← 뒤로
  ArrowRight,     // → 앞으로
  ArrowUp,        // ↑ 위로
  ArrowDown,      // ↓ 아래로
  Plus,           // + 추가
  Minus,          // - 빼기
  Search,         // 🔍 검색
  Settings,       // ⚙️ 설정
  MoreVertical,   // ⋮ 더보기 (세로)
  MoreHorizontal, // ⋯ 더보기 (가로)
  Filter,         // 필터
  Grid,           // 그리드
  List,           // 리스트
  Maximize,       // 최대화
  Minimize,       // 최소화
} from 'lucide-react'
```

### 2. 사용자 & 계정

```javascript
import {
  User,           // 👤 사용자
  Users,          // 👥 사용자들
  UserPlus,       // 사용자 추가
  UserMinus,      // 사용자 제거
  UserCheck,      // 사용자 확인
  UserX,          // 사용자 거부
  LogIn,          // 로그인
  LogOut,         // 로그아웃
  Lock,           // 🔒 잠금
  Unlock,         // 🔓 열림
  Key,            // 🔑 키
  Shield,         // 🛡️ 보안
  ShieldCheck,    // 보안 확인
  ShieldAlert,    // 보안 경고
  Eye,            // 👁️ 보기
  EyeOff,         // 👁️‍🗨️ 숨기기
} from 'lucide-react'
```

### 3. 쇼핑 & 커머스

```javascript
import {
  ShoppingCart,   // 🛒 장바구니
  ShoppingBag,    // 🛍️ 쇼핑백
  Package,        // 📦 패키지
  Box,            // 📦 박스
  Gift,           // 🎁 선물
  Tag,            // 🏷️ 태그
  CreditCard,     // 💳 신용카드
  DollarSign,     // $ 달러
  TrendingUp,     // 📈 상승
  TrendingDown,   // 📉 하락
  Percent,        // % 퍼센트
  Receipt,        // 영수증
  Wallet,         // 지갑
} from 'lucide-react'
```

### 4. 파일 & 문서

```javascript
import {
  File,           // 📄 파일
  FileText,       // 📄 텍스트 파일
  FilePlus,       // 파일 추가
  FileEdit,       // 파일 편집
  Folder,         // 📁 폴더
  FolderPlus,     // 폴더 추가
  FolderOpen,     // 📂 열린 폴더
  Download,       // ⬇️ 다운로드
  Upload,         // ⬆️ 업로드
  Save,           // 💾 저장
  Copy,           // 📋 복사
  Clipboard,      // 📋 클립보드
  Paperclip,      // 📎 첨부
  Trash,          // 🗑️ 삭제
  Trash2,         // 🗑️ 삭제 (다른 스타일)
} from 'lucide-react'
```

### 5. 통신 & 소셜

```javascript
import {
  MessageSquare,  // 💬 메시지
  MessageCircle,  // 💬 메시지 (원형)
  Mail,           // ✉️ 메일
  Send,           // ✈️ 전송
  Phone,          // 📞 전화
  PhoneCall,      // 📞 전화 중
  Video,          // 📹 비디오
  Bell,           // 🔔 알림
  BellOff,        // 🔕 알림 끄기
  Heart,          // ❤️ 좋아요
  Star,           // ⭐ 별
  Share,          // 공유
  Share2,         // 공유 (다른 스타일)
  ThumbsUp,       // 👍 좋아요
  ThumbsDown,     // 👎 싫어요
} from 'lucide-react'
```

### 6. 미디어 & 재생

```javascript
import {
  Play,           // ▶️ 재생
  Pause,          // ⏸️ 일시정지
  SkipForward,    // ⏭️ 다음
  SkipBack,       // ⏮️ 이전
  Volume,         // 🔊 볼륨
  Volume2,        // 🔊 볼륨 (다른 스타일)
  VolumeX,        // 🔇 음소거
  Mic,            // 🎤 마이크
  MicOff,         // 🎤 마이크 끄기
  Camera,         // 📷 카메라
  Image,          // 🖼️ 이미지
  Film,           // 🎬 영화
  Music,          // 🎵 음악
} from 'lucide-react'
```

### 7. 날짜 & 시간

```javascript
import {
  Calendar,       // 📅 달력
  Clock,          // 🕐 시계
  AlarmClock,     // ⏰ 알람
  Timer,          // ⏱️ 타이머
  Sunrise,        // 🌅 일출
  Sunset,         // 🌇 일몰
  Sun,            // ☀️ 태양
  Moon,           // 🌙 달
} from 'lucide-react'
```

### 8. 위치 & 지도

```javascript
import {
  MapPin,         // 📍 위치
  Map,            // 🗺️ 지도
  Navigation,     // 🧭 네비게이션
  Compass,        // 🧭 나침반
  Globe,          // 🌍 지구
  Anchor,         // ⚓ 앵커
} from 'lucide-react'
```

### 9. 날씨

```javascript
import {
  Cloud,          // ☁️ 구름
  CloudRain,      // 🌧️ 비
  CloudSnow,      // 🌨️ 눈
  CloudLightning, // ⚡ 번개
  Wind,           // 💨 바람
  Droplet,        // 💧 물방울
  Thermometer,    // 🌡️ 온도계
} from 'lucide-react'
```

### 10. 개발 & 코드

```javascript
import {
  Code,           // </> 코드
  Code2,          // </> 코드 (다른 스타일)
  Terminal,       // 터미널
  Database,       // 🗄️ 데이터베이스
  Server,         // 🖥️ 서버
  HardDrive,      // 💽 하드드라이브
  Cpu,            // CPU
  Monitor,        // 🖥️ 모니터
  Smartphone,     // 📱 스마트폰
  Tablet,         // 📱 태블릿
  Laptop,         // 💻 노트북
  Wifi,           // 📶 와이파이
  WifiOff,        // 와이파이 끄기
  Bluetooth,      // 🔵 블루투스
  USB,            // USB
  Zap,            // ⚡ 전기
  Power,          // 🔌 전원
  Battery,        // 🔋 배터리
  BatteryCharging,// 🔋 충전 중
} from 'lucide-react'
```

### 11. 비즈니스 & 오피스

```javascript
import {
  Briefcase,      // 💼 서류가방
  Building,       // 🏢 빌딩
  Building2,      // 🏢 빌딩 (다른 스타일)
  Store,          // 🏪 상점
  Flag,           // 🚩 깃발
  Target,         // 🎯 타겟
  Award,          // 🏆 상
  Medal,          // 🏅 메달
  Trophy,         // 🏆 트로피
  Bookmark,       // 🔖 북마크
  BookOpen,       // 📖 책
  GraduationCap,  // 🎓 졸업모
  Ruler,          // 📏 자
  Scissors,       // ✂️ 가위
  Pencil,         // ✏️ 연필
  Edit,           // ✏️ 편집
  Edit2,          // ✏️ 편집 (다른 스타일)
  Edit3,          // ✏️ 편집 (또 다른 스타일)
} from 'lucide-react'
```

### 12. 상태 & 피드백

```javascript
import {
  Check,          // ✓ 체크
  CheckCircle,    // ✓ 체크 (원형)
  CheckCircle2,   // ✓ 체크 (원형, 다른 스타일)
  CheckSquare,    // ✓ 체크 (사각형)
  X,              // ✕ X
  XCircle,        // ✕ X (원형)
  XSquare,        // ✕ X (사각형)
  AlertCircle,    // ⚠️ 경고 (원형)
  AlertTriangle,  // ⚠️ 경고 (삼각형)
  Info,           // ℹ️ 정보
  HelpCircle,     // ❓ 도움말
  Loader,         // ⏳ 로딩
  Loader2,        // ⏳ 로딩 (다른 스타일)
  RefreshCw,      // 🔄 새로고침
  RotateCw,       // 🔄 회전 (시계방향)
  RotateCcw,      // 🔄 회전 (반시계방향)
} from 'lucide-react'
```

### 13. 도형 & 디자인

```javascript
import {
  Circle,         // ⭕ 원
  Square,         // ⬜ 사각형
  Triangle,       // 🔺 삼각형
  Star,           // ⭐ 별
  Heart,          // ❤️ 하트
  Layers,         // 레이어
  Layout,         // 레이아웃
  Columns,        // 컬럼
  Sidebar,        // 사이드바
  PanelLeft,      // 왼쪽 패널
  PanelRight,     // 오른쪽 패널
  PanelTop,       // 위쪽 패널
  PanelBottom,    // 아래쪽 패널
  Palette,        // 🎨 팔레트
  Pipette,        // 스포이드
  Droplets,       // 물방울들
} from 'lucide-react'
```

### 14. 데이터 & 차트

```javascript
import {
  BarChart,       // 📊 막대 차트
  BarChart2,      // 📊 막대 차트 (다른 스타일)
  BarChart3,      // 📊 막대 차트 (또 다른 스타일)
  LineChart,      // 📈 선 차트
  PieChart,       // 🥧 파이 차트
  Activity,       // 📊 활동
  TrendingUp,     // 📈 상승
  TrendingDown,   // 📉 하락
  GitBranch,      // Git 브랜치
  GitCommit,      // Git 커밋
  GitMerge,       // Git 병합
} from 'lucide-react'
```

### 15. 교통 & 이동

```javascript
import {
  Car,            // 🚗 자동차
  Bike,           // 🚲 자전거
  Bus,            // 🚌 버스
  Train,          // 🚂 기차
  Plane,          // ✈️ 비행기
  Ship,           // 🚢 배
  Rocket,         // 🚀 로켓
  Truck,          // 🚛 트럭
} from 'lucide-react'
```

### 16. 자연 & 동물

```javascript
import {
  TreePine,       // 🌲 소나무
  Trees,          // 🌳 나무들
  Flower,         // 🌸 꽃
  Leaf,           // 🍃 잎
  Bug,            // 🐛 벌레
  Bird,           // 🐦 새
  Fish,           // 🐟 물고기
  Paw,            // 🐾 발자국
} from 'lucide-react'
```

### 17. 음식 & 음료

```javascript
import {
  Coffee,         // ☕ 커피
  Pizza,          // 🍕 피자
  Apple,          // 🍎 사과
  Cherry,         // 🍒 체리
  Wine,           // 🍷 와인
  Beer,           // 🍺 맥주
  Utensils,       // 🍴 식기
  UtensilsCrossed,// 🍴 교차된 식기
} from 'lucide-react'
```

### 18. 스포츠 & 게임

```javascript
import {
  Gamepad,        // 🎮 게임패드
  Gamepad2,       // 🎮 게임패드 (다른 스타일)
  Dices,          // 🎲 주사위
  Swords,         // ⚔️ 검
  Flame,          // 🔥 불
  Zap,            // ⚡ 번개
  Crown,          // 👑 왕관
} from 'lucide-react'
```

---

## 💻 사용법

### 기본 사용

```jsx
import { Home, User, Settings } from 'lucide-react'

function MyComponent() {
  return (
    <div>
      <Home />
      <User />
      <Settings />
    </div>
  )
}
```

### Props 사용

```jsx
<Home 
  size={24}              // 크기
  color="blue"           // 색상
  strokeWidth={2}        // 선 굵기
  className="my-icon"    // CSS 클래스
  style={{ margin: 10 }} // 인라인 스타일
/>
```

### 크기 옵션

```jsx
<Home size={16} />  // 작게
<Home size={24} />  // 기본
<Home size={32} />  // 중간
<Home size={48} />  // 크게
```

### 색상 지정

```jsx
<Home color="red" />
<Home color="#3B82F6" />
<Home color="rgb(59, 130, 246)" />
<Home className="text-blue-500" />  // Tailwind
```

### 애니메이션

```jsx
// Tailwind CSS로 애니메이션
<Home className="hover:rotate-12 transition-transform" />
<Loader2 className="animate-spin" />

// 인라인 스타일로 애니메이션
<Home 
  style={{ 
    transition: 'transform 0.3s',
    cursor: 'pointer'
  }}
  onMouseEnter={(e) => e.currentTarget.style.transform = 'scale(1.2)'}
  onMouseLeave={(e) => e.currentTarget.style.transform = 'scale(1)'}
/>
```

---

## 🎯 실제 사용 예시

### Navbar 예시

```jsx
import { 
  Home, ShoppingCart, User, LogOut, 
  MessageSquare, Package, Settings 
} from 'lucide-react'

function Navbar() {
  return (
    <nav>
      <a href="/"><Home size={20} /> Home</a>
      <a href="/products"><Package size={20} /> Products</a>
      <a href="/cart"><ShoppingCart size={20} /> Cart</a>
      <a href="/messages"><MessageSquare size={20} /> Messages</a>
      <a href="/profile"><User size={20} /> Profile</a>
      <a href="/settings"><Settings size={20} /> Settings</a>
      <button><LogOut size={20} /> Logout</button>
    </nav>
  )
}
```

### 버튼 예시

```jsx
import { Plus, Trash2, Edit, Save } from 'lucide-react'

function ActionButtons() {
  return (
    <div className="flex gap-2">
      <button className="btn-primary">
        <Plus size={18} /> Add New
      </button>
      <button className="btn-success">
        <Save size={18} /> Save
      </button>
      <button className="btn-warning">
        <Edit size={18} /> Edit
      </button>
      <button className="btn-danger">
        <Trash2 size={18} /> Delete
      </button>
    </div>
  )
}
```

### 상태 표시 예시

```jsx
import { CheckCircle, XCircle, AlertCircle, Loader2 } from 'lucide-react'

function StatusIndicator({ status }) {
  const icons = {
    success: <CheckCircle color="green" />,
    error: <XCircle color="red" />,
    warning: <AlertCircle color="orange" />,
    loading: <Loader2 className="animate-spin" />
  }
  
  return <div>{icons[status]}</div>
}
```

### 드롭다운 메뉴

```jsx
import { ChevronDown, User, Settings, LogOut } from 'lucide-react'

function DropdownMenu() {
  return (
    <div className="dropdown">
      <button>
        <User size={20} />
        <ChevronDown size={16} />
      </button>
      <div className="dropdown-menu">
        <a href="/profile"><User size={16} /> Profile</a>
        <a href="/settings"><Settings size={16} /> Settings</a>
        <hr />
        <a href="/logout"><LogOut size={16} /> Logout</a>
      </div>
    </div>
  )
}
```

---

## 📚 전체 아이콘 목록

**공식 사이트**: https://lucide.dev/icons/

**총 개수**: 1000+ 아이콘

**검색 방법**:
1. https://lucide.dev 접속
2. 검색창에 원하는 키워드 입력 (예: "arrow", "user", "file")
3. 아이콘 클릭 → React 컴포넌트 이름 확인

---

## 🎨 스타일링 팁

### Tailwind CSS와 함께

```jsx
<Home className="w-6 h-6 text-blue-500 hover:text-blue-700" />
<ShoppingCart className="text-red-500 animate-bounce" />
```

### 인라인 스타일

```jsx
<User 
  size={24} 
  style={{ 
    color: '#3B82F6',
    cursor: 'pointer',
    transition: 'all 0.3s'
  }} 
/>
```

### CSS 클래스

```css
.icon-primary {
  color: #3B82F6;
  width: 24px;
  height: 24px;
}

.icon-primary:hover {
  color: #2563EB;
  transform: scale(1.1);
}
```

```jsx
<Settings className="icon-primary" />
```

---

## 🔥 자주 사용하는 조합

### 관리자 패널

```jsx
import { 
  LayoutDashboard, Users, Settings, BarChart, 
  Database, Shield, FileText, Activity 
} from 'lucide-react'
```

### 이커머스

```jsx
import { 
  ShoppingCart, Package, CreditCard, Truck, 
  Gift, Tag, Star, Heart 
} from 'lucide-react'
```

### 소셜 미디어

```jsx
import { 
  MessageCircle, Heart, Share2, Send, 
  ThumbsUp, Bell, User, Camera 
} from 'lucide-react'
```

### 개발 도구

```jsx
import { 
  Code, Terminal, Database, Server, 
  GitBranch, Bug, Zap, Settings 
} from 'lucide-react'
```

---

## 💡 Pro Tips

1. **일관성**: 프로젝트 전체에서 같은 크기 사용 (예: 20px)
2. **접근성**: 아이콘에 aria-label 추가
3. **성능**: 필요한 아이콘만 import
4. **커스터마이징**: strokeWidth로 디자인에 맞게 조정

```jsx
<Home 
  size={20} 
  strokeWidth={1.5}  // 가는 선
  aria-label="홈으로 이동"
/>
```

이제 1000개 이상의 Lucide 아이콘을 마음껏 사용하세요! 🎉
